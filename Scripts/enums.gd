extends Node

enum task {	idle,
			seek_target,
			go_target,
			harvest,
			seek_home,
			go_home,
			unload,
			cultivate,
			research,
			operate,
			}

enum jobs { 	jobless,
				woodcutter,
				peasant,
				defender,
				scientist
			}
